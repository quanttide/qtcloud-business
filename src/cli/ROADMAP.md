# CLI ROADMAP

## 下一步

- [ ] 把 `src/cli/src/business/quote.rs` 拆成解析、计算、格式化三个小模块，降低后续维护成本。
- [ ] 把 quote 计算核心抽成共享领域模块，给未来服务端复用。
- [ ] 增加报价存储和历史记录导出能力。
- [ ] 增加 CLI 命令级文档与固定输出样例，稳定回归测试。
- [ ] 等服务端落地后，让 CLI 通过服务层取数与计算。
